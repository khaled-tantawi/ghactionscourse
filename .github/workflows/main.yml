name : First Workflow

on:
  push: 
    branches:
      - main

jobs:
  example-job:
    run-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo "welcome to our first githubactions workflow"
